# Read-only Location

1. **File Explorer** 先读取 `.ai-work-flow/index/feature-navigation.md`，再读取目标层索引。
2. 命中用户术语后只打开记录入口及完成请求所需的直接 import、caller 或 schema。
3. 只有索引缺失、未命中或路径失效时使用聚焦 `rg`。
4. 返回精确入口、直接依赖、模块边界和验证事实；不得编辑或因过期自行维护。
