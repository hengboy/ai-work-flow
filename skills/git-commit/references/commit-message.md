# Commit Message

使用 Conventional Commits 1.0.0：`<type>(<optional scope>): <中文摘要>`。type 使用 `feat`、`fix`、`docs`、`test`、`refactor`、`build`、`ci`、`chore` 中最准确的一项；摘要描述结果，不写过程，不以句号结尾。需要正文时用中文说明动机与验证；破坏性变更使用 `!` 和 `BREAKING CHANGE:`。
