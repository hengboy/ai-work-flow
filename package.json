{
  "name": "ai-work-flow",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm run check:runtime --prefix skills/run-matt-spec-to-completion",
    "test": "node --test"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "yaml": "^2.9.0"
  }
}
